package grupa5;


import javafx.fxml.FXML;
import javafx.scene.layout.VBox;

public class ReservedCardController {


    // This is the controller class for EventCard.fxml.
    // Currently, it doesn't need to perform any actions,
    // but it should be able to display content via the associated FXML file.
}